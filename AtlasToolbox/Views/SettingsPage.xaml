<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AtlasToolbox.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AtlasToolbox.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls" 
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="CategoryTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
        </Style>
    </Page.Resources>
    <Grid Margin="55 0 0 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock
             Grid.Row="0"
             x:Name="TitleTxt"
             Margin="0,24,36,24"
             Style="{StaticResource TitleTextBlockStyle}"
            Text="Settings"/>
        <StackPanel Grid.Row="1">
            <TextBlock Style="{StaticResource CategoryTitle}" Text="Behavior"/>
            <controls:SettingsCard Style="{StaticResource ConfigurationSettingsCardTemplate}" Header="Keep the Toolbox in the background">
                <ToggleSwitch Toggled="KeepBackground_Toggled" IsOn="{x:Bind KeepBackground_State}"/>
            </controls:SettingsCard>
        </StackPanel>
    </Grid>
</Page>