<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:model="using:AtlasToolbox.Models">
    <DataTemplate x:Key="ControlItemTemplate" x:DataType="model:Configuration">
        <Grid
            x:Name="controlRoot"
            Width="300"
            Height="96"
            Padding="8"
            HorizontalAlignment="Stretch"
            Background="{ThemeResource ControlFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            ColumnSpacing="16"
            CornerRadius="{StaticResource OverlayCornerRadius}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Image
                    x:Name="gridImage"
                    Grid.RowSpan="2"
                    Width="32"
                    Margin="8,12,16,0"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="{x:Bind Name}"
                    Source="C:\Users\TheyCreeper\Documents\Dev\atlas-toolbox\AtlasToolbox\assets\Logo\atlas-logo.png"
                    Stretch="Uniform" />
                <TextBlock
                    x:Name="titleText"
                    Grid.Column="1"
                    Margin="0,12,0,0"
                    VerticalAlignment="Bottom"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    Text="{x:Bind Name}"
                    TextLineBounds="TrimToCapHeight"
                    TextWrapping="NoWrap" />
            </Grid>
        </Grid>
    </DataTemplate>
</ResourceDictionary>
